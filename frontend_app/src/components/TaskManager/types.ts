export type CustomTask = { 
    id: string;
    title: string;
    description: string;
    status: string;
    parent: string;
};
  
export type CustomBaseTask = {
  title: string;
  description: string;
  parent: string;
};
